# Normalize line endings: store everything as LF in the repo, regardless of
# what the working tree has. Prevents CRLF (Windows / Dropbox sync) churn from
# showing every file as modified. Binary files are auto-detected and left alone.
* text=auto eol=lf
